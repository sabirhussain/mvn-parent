# Default container registry settings
-Dcontainer.registry=docker.io
-Dcontainer.organization=change-me

# Sonar for local
-Dsonar.host.url=http://localhost:9000
-Dsonar.projectKey=${project.groupId}:${project.artifactId}

# Optional: Performance tuning for Maven builds
-T 1C
--batch-mode