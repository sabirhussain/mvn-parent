# Default container registry settings
-Dcontainer.registry={{REGISTRY}}
-Dcontainer.organization={{ORGANIZATION}}

# SonarQube configuration (optional - uncomment and configure via additional-setup.sh)
# -Dsonar.host.url=http://localhost:9000
# -Dsonar.projectKey=${project.groupId}:${project.artifactId}

# Optional: Activate default profiles
-Pcoverage

# Optional: Performance tuning
-T 1C
--batch-mode
